import 'package:flutter/material.dart';

class DaftarLaporanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Daftar Laporan')),
      body: Center(child: Text('Daftar semua laporan akan muncul di sini')),
    );
  }
}
